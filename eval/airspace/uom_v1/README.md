Recorded UOM answers for WP-M3-19 (synthetic fixtures shaped like UOM responses, not real filings). Each
sub-directory is one recorded context; files are named <volume_id>.json. Used only by tests and never by a
flight. / WP-M3-19 的录制 UOM 应答（按 UOM 应答形状构造的合成夹具，不是真实报备）。每个子目录是一种录制情形，
文件名为 <volume_id>.json。只供测试使用，从不用于飞行。
