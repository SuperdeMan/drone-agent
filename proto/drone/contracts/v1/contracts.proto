// Generated by scripts/generate_contract_fields.py; review before freezing v1.
// 由 scripts/generate_contract_fields.py 生成；冻结 v1 前必须审查。
syntax = "proto3";
package drone.contracts.v1;
import "google/protobuf/timestamp.proto";

enum ControlMode {
  CONTROL_MODE_UNSPECIFIED = 0;
  CONTROL_MODE_MISSION_UPLOAD = 1;
  CONTROL_MODE_OFFBOARD_POSITION = 2;
  CONTROL_MODE_OFFBOARD_VELOCITY = 3;
  CONTROL_MODE_OFFBOARD_TRAJECTORY = 4;
  CONTROL_MODE_EXTERNAL_MODE = 5;
  CONTROL_MODE_NAV_TO_POSE = 6;
  CONTROL_MODE_NONE = 7;
}

enum EffectVerdict {
  EFFECT_VERDICT_UNSPECIFIED = 0;
  EFFECT_VERDICT_VERIFIED = 1;
  EFFECT_VERDICT_UNVERIFIED = 2;
  EFFECT_VERDICT_REFUTED = 3;
  EFFECT_VERDICT_UNKNOWN = 4;
}

enum Embodiment {
  EMBODIMENT_UNSPECIFIED = 0;
  EMBODIMENT_AERIAL_MULTIROTOR = 1;
  EMBODIMENT_AERIAL_VTOL = 2;
  EMBODIMENT_AERIAL_FIXED_WING = 3;
  EMBODIMENT_GROUND_WHEELED = 4;
  EMBODIMENT_GROUND_LEGGED = 5;
  EMBODIMENT_MANIPULATOR = 6;
}

enum EventType {
  EVENT_TYPE_UNSPECIFIED = 0;
  EVENT_TYPE_COMMAND_ACCEPTED = 1;
  EVENT_TYPE_COMMAND_REJECTED = 2;
  EVENT_TYPE_SKILL_STARTED = 3;
  EVENT_TYPE_SKILL_PROGRESS = 4;
  EVENT_TYPE_SKILL_PAUSED = 5;
  EVENT_TYPE_SKILL_CANCEL_REQUESTED = 6;
  EVENT_TYPE_SKILL_COMPLETED = 7;
  EVENT_TYPE_SKILL_FAILED = 8;
  EVENT_TYPE_EFFECT_VERIFIED = 9;
  EVENT_TYPE_EFFECT_REFUTED = 10;
  EVENT_TYPE_EFFECT_UNKNOWN = 11;
  EVENT_TYPE_SAFETY_INTERVENTION = 12;
  EVENT_TYPE_LEASE_ISSUED = 13;
  EVENT_TYPE_LEASE_EXPIRED = 14;
  EVENT_TYPE_LEASE_REVOKED = 15;
  EVENT_TYPE_RECOVERED_TO = 16;
  EVENT_TYPE_HANDOFF_OFFERED = 17;
  EVENT_TYPE_HANDOFF_ACCEPTED = 18;
  EVENT_TYPE_HANDOFF_REJECTED = 19;
  EVENT_TYPE_HANDOFF_COMPLETED = 20;
  EVENT_TYPE_HANDOFF_TIMEOUT = 21;
  EVENT_TYPE_MISSION_ACCEPTED = 22;
  EVENT_TYPE_MISSION_FINISHED = 23;
  EVENT_TYPE_OPERATOR_REQUEST_ACCEPTED = 24;
  EVENT_TYPE_OPERATOR_REQUEST_REJECTED = 25;
  EVENT_TYPE_RUNTIME_RECORD = 26;
}

enum ExecutionStatus {
  EXECUTION_STATUS_UNSPECIFIED = 0;
  EXECUTION_STATUS_PENDING = 1;
  EXECUTION_STATUS_ACCEPTED = 2;
  EXECUTION_STATUS_RUNNING = 3;
  EXECUTION_STATUS_SUCCEEDED = 4;
  EXECUTION_STATUS_FAILED = 5;
  EXECUTION_STATUS_CANCELLED = 6;
  EXECUTION_STATUS_TIMEOUT = 7;
  EXECUTION_STATUS_UNKNOWN = 8;
}

enum FactSource {
  FACT_SOURCE_UNSPECIFIED = 0;
  FACT_SOURCE_SENSOR = 1;
  FACT_SOURCE_MODEL = 2;
  FACT_SOURCE_HUMAN = 3;
  FACT_SOURCE_SIM_TRUTH = 4;
}

enum GoalType {
  GOAL_TYPE_UNSPECIFIED = 0;
  GOAL_TYPE_INSPECT = 1;
  GOAL_TYPE_SURVEY = 2;
  GOAL_TYPE_DELIVER = 3;
  GOAL_TYPE_SEARCH = 4;
  GOAL_TYPE_RECHECK = 5;
  GOAL_TYPE_OTHER = 6;
}

enum ImplementationKind {
  IMPLEMENTATION_KIND_UNSPECIFIED = 0;
  IMPLEMENTATION_KIND_DETERMINISTIC = 1;
  IMPLEMENTATION_KIND_LEARNED = 2;
}

enum LearnedStage {
  LEARNED_STAGE_UNSPECIFIED = 0;
  LEARNED_STAGE_SHADOW = 1;
  LEARNED_STAGE_LIMITED = 2;
  LEARNED_STAGE_FULL = 3;
}

enum MissionAction {
  MISSION_ACTION_UNSPECIFIED = 0;
  MISSION_ACTION_PAUSE = 1;
  MISSION_ACTION_RESUME = 2;
  MISSION_ACTION_CANCEL = 3;
}

enum RecoveryBehavior {
  RECOVERY_BEHAVIOR_UNSPECIFIED = 0;
  RECOVERY_BEHAVIOR_HOLD = 1;
  RECOVERY_BEHAVIOR_LOITER = 2;
  RECOVERY_BEHAVIOR_RTL = 3;
  RECOVERY_BEHAVIOR_LAND_HERE = 4;
  RECOVERY_BEHAVIOR_LAND_AT = 5;
  RECOVERY_BEHAVIOR_HANDOVER_TO_FC_FAILSAFE = 6;
}

enum RecoveryTrigger {
  RECOVERY_TRIGGER_UNSPECIFIED = 0;
  RECOVERY_TRIGGER_EXECUTIVE_HEARTBEAT_LOST = 1;
  RECOVERY_TRIGGER_OBSERVATION_STALE = 2;
  RECOVERY_TRIGGER_TRAJECTORY_STALE = 3;
  RECOVERY_TRIGGER_LEASE_EXPIRED = 4;
  RECOVERY_TRIGGER_ENERGY_LOW = 5;
  RECOVERY_TRIGGER_GEOFENCE_PREDICTED_BREACH = 6;
  RECOVERY_TRIGGER_LOCALIZATION_DEGRADED = 7;
  RECOVERY_TRIGGER_UPLINK_LOST = 8;
  RECOVERY_TRIGGER_FC_FAILSAFE_ACTIVE = 9;
  RECOVERY_TRIGGER_USER_PAUSE = 10;
  RECOVERY_TRIGGER_USER_CANCEL = 11;
  RECOVERY_TRIGGER_PROGRESS_STALLED = 12;
  RECOVERY_TRIGGER_COMPUTE_OVERLOADED = 13;
  RECOVERY_TRIGGER_AUTONOMY_UNAVAILABLE = 14;
}

enum ResourceMode {
  RESOURCE_MODE_UNSPECIFIED = 0;
  RESOURCE_MODE_EXCLUSIVE = 1;
  RESOURCE_MODE_SHARED = 2;
}

enum SafetyVerdict {
  SAFETY_VERDICT_UNSPECIFIED = 0;
  SAFETY_VERDICT_PROCEED = 1;
  SAFETY_VERDICT_HOLD = 2;
  SAFETY_VERDICT_RECOVER = 3;
  SAFETY_VERDICT_ABORT = 4;
}

enum SkillInstanceState {
  SKILL_INSTANCE_STATE_UNSPECIFIED = 0;
  SKILL_INSTANCE_STATE_ACCEPTED = 1;
  SKILL_INSTANCE_STATE_PREPARING = 2;
  SKILL_INSTANCE_STATE_RUNNING = 3;
  SKILL_INSTANCE_STATE_VERIFYING = 4;
  SKILL_INSTANCE_STATE_COMPLETED = 5;
  SKILL_INSTANCE_STATE_PAUSE_REQUESTED = 6;
  SKILL_INSTANCE_STATE_PAUSED = 7;
  SKILL_INSTANCE_STATE_CANCEL_REQUESTED = 8;
  SKILL_INSTANCE_STATE_RECOVERING = 9;
  SKILL_INSTANCE_STATE_CANCELLED = 10;
  SKILL_INSTANCE_STATE_BLOCKED = 11;
  SKILL_INSTANCE_STATE_FAILED = 12;
  SKILL_INSTANCE_STATE_OUTCOME_UNKNOWN = 13;
}

enum WorldKind {
  WORLD_KIND_UNSPECIFIED = 0;
  WORLD_KIND_TRUTH = 1;
  WORLD_KIND_BELIEF = 2;
  WORLD_KIND_PREDICTED = 3;
}

message AerialState {
  optional string schema_version = 1;
  optional ObservationHeader header = 2;
  optional Pose pose = 3;
  repeated double velocity_mps = 4;
  optional string flight_phase = 5;
  optional ControlMode control_mode = 6;
  optional bool armed = 7;
  optional bool fc_failsafe_active = 8;
  optional LocalizationHealth localization = 9;
  optional EnergyState energy = 10;
}

message ApprovalRecord {
  optional string schema_version = 1;
  optional string approver = 2;
  optional google.protobuf.Timestamp approved_at = 3;
  optional string mission_id = 4;
  optional int64 mission_version = 5;
  optional string package_hash = 6;
  optional google.protobuf.Timestamp expires_at = 7;
  repeated string allowed_robots = 8;
  optional string signature = 9;
  optional string signer_key_id = 10;
}

message CancelSpec {
  optional string schema_version = 1;
  repeated string cancel_safe_states = 2;
  optional string cancel_procedure = 3;
}

message CapabilityDescriptor {
  optional string schema_version = 1;
  optional string robot_id = 2;
  optional Embodiment embodiment = 3;
  optional string interface_version = 4;
  repeated ControlMode control_modes = 5;
  repeated SkillRef skills = 6;
  repeated SensorSpec sensors = 7;
  repeated PayloadSpec payloads = 8;
  optional Limits limits = 9;
  repeated RecoveryBehavior recovery_behaviors = 10;
  optional string vendor = 11;
  optional string model = 12;
  repeated string vendor_constraints = 13;
}

message CollaborationRule {
  optional string schema_version = 1;
  optional string trigger = 2;
  optional string required_skill = 3;
  repeated Embodiment required_embodiments = 4;
  optional double handoff_timeout_s = 5;
}

message CommsState {
  optional string schema_version = 1;
  optional bool uplink_ok = 2;
  optional double latency_ms = 3;
  optional google.protobuf.Timestamp last_seen = 4;
}

message ControlCommandEnvelope {
  optional string schema_version = 1;
  optional IdempotencyKey key = 2;
  optional int64 command_seq = 3;
  optional google.protobuf.Timestamp issued_at = 4;
  optional google.protobuf.Timestamp valid_until = 5;
  optional string intent_kind = 6;
  // UTF-8 JSON; validate after decoding. / UTF-8 JSON；解码后重新校验。
  optional bytes payload = 7;
}

message EgressDecision {
  optional string schema_version = 1;
  optional bool accepted = 2;
  optional string reason = 3;
}

message EnergyBudget {
  optional string schema_version = 1;
  optional double max_consumption_fraction = 2;
  optional double reserve_fraction = 3;
}

message EnergyEstimate {
  optional string schema_version = 1;
  optional string basis = 2;
  optional double mean_fraction = 3;
  optional double upper_fraction = 4;
  optional int64 samples = 5;
  optional string method = 6;
  optional string source_revision = 7;
  repeated string source_runs = 8;
}

message EnergyState {
  optional string schema_version = 1;
  optional double remaining_fraction = 2;
  optional double remaining_energy_wh = 3;
  optional double reserve_required_fraction = 4;
}

message Evidence {
  optional string schema_version = 1;
  optional string evidence_id = 2;
  optional string kind = 3;
  optional string media_ref = 4;
  optional string sha256 = 5;
  optional TimeWindow time_window = 6;
  optional Pose captured_pose = 7;
  repeated string subject_ids = 8;
  // UTF-8 JSON; validate after decoding. / UTF-8 JSON；解码后重新校验。
  optional bytes quality = 9;
  optional string produced_by_skill_instance = 10;
  optional string mission_id = 11;
  optional int64 mission_version = 12;
  optional string robot_id = 13;
}

message EvidenceRequirement {
  optional string schema_version = 1;
  optional string evidence_type = 2;
  // UTF-8 JSON; validate after decoding. / UTF-8 JSON；解码后重新校验。
  optional bytes criteria = 3;
}

message ExecutionEvent {
  optional string schema_version = 1;
  optional string event_id = 2;
  optional EventType event_type = 3;
  optional google.protobuf.Timestamp timestamp = 4;
  optional string mission_id = 5;
  optional int64 mission_version = 6;
  optional string robot_id = 7;
  optional string step_id = 8;
  optional string command_id = 9;
  optional int64 lease_epoch = 10;
  optional ExecutionStatus execution_status = 11;
  optional EffectVerdict effect_verdict = 12;
  optional SafetyVerdict safety_verdict = 13;
  optional string reason = 14;
  repeated string evidence_refs = 15;
  // UTF-8 JSON; validate after decoding. / UTF-8 JSON；解码后重新校验。
  optional bytes data = 16;
}

message FailureMode {
  optional string schema_version = 1;
  optional string code = 2;
  optional string description = 3;
  optional string recovery_hint = 4;
}

message FlightObservation {
  optional string schema_version = 1;
  optional google.protobuf.Timestamp timestamp = 2;
  optional google.protobuf.Timestamp valid_until = 3;
  optional int64 sample_id = 4;
  optional string robot_id = 5;
  optional Pose pose = 6;
  repeated double velocity_enu_mps = 7;
  optional bool armed = 8;
  optional bool in_air = 9;
  optional string flight_mode = 10;
  optional bool localization_healthy = 11;
  optional bool home_healthy = 12;
  optional double battery_fraction = 13;
  optional int64 mission_current = 14;
  optional int64 mission_total = 15;
  optional bool fc_failsafe = 16;
  optional string source = 17;
}

message Frame {
  optional string schema_version = 1;
  optional string frame_id = 2;
  optional string map_version = 3;
}

message GroundMobileState {
  optional string schema_version = 1;
  optional ObservationHeader header = 2;
  optional Pose pose = 3;
  repeated double velocity_mps = 4;
  optional bool traversability_ok = 5;
  optional LocalizationHealth localization = 6;
  optional EnergyState energy = 7;
}

message IdempotencyKey {
  optional string schema_version = 1;
  optional string mission_id = 2;
  optional int64 mission_version = 3;
  optional string step_id = 4;
  optional string command_id = 5;
  optional string robot_id = 6;
  optional int64 lease_epoch = 7;
}

message Implementation {
  optional string schema_version = 1;
  optional ImplementationKind kind = 2;
  optional LearnedStage learned_stage = 3;
  optional string model_version = 4;
}

message IntentPhase {
  optional string schema_version = 1;
  optional string phase = 2;
  repeated string preconditions = 3;
}

message Limits {
  optional string schema_version = 1;
  optional double max_speed_mps = 2;
  optional double max_altitude_m_agl = 3;
  optional double endurance_s = 4;
  optional double max_wind_mps = 5;
  optional double max_payload_kg = 6;
  optional double min_turn_radius_m = 7;
  optional bool can_hover = 8;
}

message LocalizationHealth {
  optional string schema_version = 1;
  optional string fix_type = 2;
  optional double position_std_m = 3;
  optional bool healthy = 4;
}

message MissionOperation {
  optional string schema_version = 1;
  optional string robot_id = 2;
  optional string mission_id = 3;
  optional int64 mission_version = 4;
  optional int64 lease_epoch = 5;
  optional string executive_instance = 6;
  optional MissionAction action = 7;
  optional string request_id = 8;
}

message MissionPackage {
  optional string schema_version = 1;
  optional string mission_id = 2;
  optional int64 mission_version = 3;
  repeated PackageNode nodes = 4;
  optional string recovery_policy_ref = 5;
  optional ApprovalRecord approval = 6;
  optional string package_hash = 7;
  optional SpatialScope spatial_scope = 8;
  optional TemporalWindow temporal_window = 9;
  optional EnergyBudget energy_budget = 10;
}

message MissionSpec {
  optional string schema_version = 1;
  optional string mission_id = 2;
  optional int64 mission_version = 3;
  optional string goal = 4;
  optional GoalType goal_type = 5;
  repeated TargetRef targets = 6;
  optional SpatialScope spatial_scope = 7;
  optional TemporalWindow temporal_window = 8;
  repeated TaskNode tasks = 9;
  optional EnergyBudget energy_budget = 10;
  optional string recovery_policy_ref = 11;
  repeated CollaborationRule collaboration_rules = 12;
  optional Provenance provenance = 13;
}

message ObservationHeader {
  optional string schema_version = 1;
  optional string robot_id = 2;
  optional google.protobuf.Timestamp timestamp = 3;
  optional google.protobuf.Timestamp valid_until = 4;
  optional Frame frame = 5;
  optional FactSource source = 6;
  optional string source_version = 7;
}

message ObservationRequest {
  optional string schema_version = 1;
  optional string robot_id = 2;
}

message OperatorRequest {
  optional string schema_version = 1;
  optional string request_id = 2;
  optional string robot_id = 3;
  optional string mission_id = 4;
  optional int64 mission_version = 5;
  optional int64 lease_epoch = 6;
  optional string step_id = 7;
  optional MissionAction action = 8;
  optional google.protobuf.Timestamp valid_until = 9;
  optional string requested_by = 10;
}

message PackageNode {
  optional string schema_version = 1;
  optional string task_id = 2;
  optional string skill_id = 3;
  optional string skill_version = 4;
  optional string robot_id = 5;
  // UTF-8 JSON; validate after decoding. / UTF-8 JSON；解码后重新校验。
  optional bytes params = 6;
  repeated string depends_on = 7;
  repeated ResourceClaim resources = 8;
  optional double timeout_s = 9;
  repeated string completion_evidence = 10;
  repeated string allow_unverified_from = 11;
}

message PauseSpec {
  optional string schema_version = 1;
  optional bool pausable = 2;
  optional string safe_wait_condition = 3;
  optional double max_wait_s = 4;
}

message PayloadSpec {
  optional string schema_version = 1;
  optional string payload_id = 2;
  optional string kind = 3;
  repeated string capabilities = 4;
}

message PlannerTool {
  optional string schema_version = 1;
  optional string name = 2;
  optional bool read_only = 3;
  optional string description = 4;
}

message PlannerToolAllowlist {
  optional string schema_version = 1;
  repeated PlannerTool tools = 2;
}

message Pose {
  optional string schema_version = 1;
  optional Frame frame = 2;
  optional Position position = 3;
  optional Quaternion orientation = 4;
}

message Position {
  optional string schema_version = 1;
  optional double x = 2;
  optional double y = 3;
  optional double z = 4;
  repeated double covariance = 5;
}

message Provenance {
  optional string schema_version = 1;
  optional string model_id = 2;
  optional string prompt_version = 3;
  optional string input_hash = 4;
  optional google.protobuf.Timestamp generated_at = 5;
}

message Quaternion {
  optional string schema_version = 1;
  optional double x = 2;
  optional double y = 3;
  optional double z = 4;
  optional double w = 5;
}

message RecoveryEdge {
  optional string schema_version = 1;
  optional RecoveryTrigger trigger = 2;
  // UTF-8 JSON; validate after decoding. / UTF-8 JSON；解码后重新校验。
  optional bytes guard = 3;
  optional RecoveryBehavior target = 4;
  optional RecoveryBehavior then = 5;
  optional double after_s = 6;
  optional string fault_injection_scenario = 7;
  optional RecoveryValidation validation = 8;
}

message RecoveryPolicy {
  optional string schema_version = 1;
  optional string policy_id = 2;
  optional string version = 3;
  optional Embodiment embodiment = 4;
  repeated RecoveryBehavior nodes = 5;
  repeated RecoveryEdge edges = 6;
  repeated string resume_conditions = 7;
}

message RecoveryValidation {
  optional string schema_version = 1;
  optional string scenario_id = 2;
  optional string edge_hash = 3;
  optional string software_revision = 4;
  optional google.protobuf.Timestamp tested_at = 5;
  optional string evidence_ref = 6;
}

message ResourceClaim {
  optional string schema_version = 1;
  optional string resource_id = 2;
  optional ResourceMode mode = 3;
  optional string shared_mode = 4;
}

message RobotStatus {
  optional string schema_version = 1;
  optional string robot_id = 2;
  optional google.protobuf.Timestamp timestamp = 3;
  optional string flight_phase = 4;
  optional ControlMode control_mode = 5;
  optional EnergyState energy = 6;
  optional LocalizationHealth localization = 7;
  optional CommsState comms = 8;
  optional int64 lease_epoch = 9;
  optional string active_skill_instance = 10;
  repeated string held_resources = 11;
  optional bool fc_failsafe_active = 12;
}

message SensorSpec {
  optional string schema_version = 1;
  optional string sensor_id = 2;
  optional string kind = 3;
  optional string frame_id = 4;
  // UTF-8 JSON; validate after decoding. / UTF-8 JSON；解码后重新校验。
  optional bytes nominal_accuracy = 5;
}

message SkillManifest {
  optional string schema_version = 1;
  optional string skill_id = 2;
  optional string version = 3;
  optional string description = 4;
  repeated Embodiment embodiments = 5;
  // UTF-8 JSON; validate after decoding. / UTF-8 JSON；解码后重新校验。
  optional bytes params_schema = 6;
  repeated string preconditions = 7;
  repeated string invariants = 8;
  repeated ResourceClaim resources = 9;
  // UTF-8 JSON; validate after decoding. / UTF-8 JSON；解码后重新校验。
  optional bytes progress_schema = 10;
  optional CancelSpec cancel = 11;
  optional PauseSpec pause = 12;
  repeated EvidenceRequirement completion_evidence = 13;
  repeated FailureMode failure_modes = 14;
  optional double timeout_s = 15;
  optional double estimated_duration_s = 16;
  optional double estimated_energy_fraction = 17;
  optional Implementation implementation = 18;
  repeated ControlMode required_control_modes = 19;
  repeated IntentPhase intent_phases = 20;
  optional EnergyEstimate energy_estimate = 21;
}

message SkillRef {
  optional string schema_version = 1;
  optional string skill_id = 2;
  optional string version = 3;
}

message SpatialScope {
  optional string schema_version = 1;
  optional string approved_volume_id = 2;
  optional Frame frame = 3;
}

message StepOutcome {
  optional string schema_version = 1;
  optional string mission_id = 2;
  optional int64 mission_version = 3;
  optional string step_id = 4;
  optional string robot_id = 5;
  optional ExecutionStatus execution_status = 6;
  optional EffectVerdict effect_verdict = 7;
  optional SafetyVerdict safety_verdict = 8;
}

message TargetRef {
  optional string schema_version = 1;
  optional string asset_id = 2;
  optional Pose pose = 3;
  optional string description = 4;
}

message TaskLease {
  optional string schema_version = 1;
  optional string robot_id = 2;
  optional string mission_id = 3;
  optional int64 mission_version = 4;
  optional int64 lease_epoch = 5;
  optional string holder = 6;
  optional google.protobuf.Timestamp issued_at = 7;
  optional google.protobuf.Timestamp expires_at = 8;
  repeated string resources = 9;
}

message TaskNode {
  optional string schema_version = 1;
  optional string task_id = 2;
  optional string skill_id = 3;
  // UTF-8 JSON; validate after decoding. / UTF-8 JSON；解码后重新校验。
  optional bytes params = 4;
  repeated string depends_on = 5;
  repeated string completion_evidence = 6;
  repeated string allow_unverified_from = 7;
}

message TemporalWindow {
  optional string schema_version = 1;
  optional google.protobuf.Timestamp not_before = 2;
  optional google.protobuf.Timestamp not_after = 3;
  optional string tz = 4;
}

message TimeWindow {
  optional string schema_version = 1;
  optional google.protobuf.Timestamp timestamp = 2;
  optional google.protobuf.Timestamp valid_until = 3;
}

message WorldFact {
  optional string schema_version = 1;
  optional string fact_id = 2;
  optional string subject = 3;
  optional string predicate = 4;
  // UTF-8 JSON; validate after decoding. / UTF-8 JSON；解码后重新校验。
  optional bytes value = 5;
  optional Frame frame = 6;
  optional Position position = 7;
  optional google.protobuf.Timestamp timestamp = 8;
  optional google.protobuf.Timestamp valid_until = 9;
  optional FactSource source = 10;
  optional string source_version = 11;
  optional double confidence = 12;
  optional WorldKind world_kind = 13;
  repeated string evidence_refs = 14;
}
