"""drone-agent: safety-constrained mission runtime for air-ground heterogeneous robots.

M0 ships only the contract layer (`drone_agent.contracts`). Modules are created per milestone
(see CLAUDE.md), never pre-built empty.

drone-agent：面向空地异构机器人的安全约束任务运行时。

M0 只交付契约层（`drone_agent.contracts`）。模块随里程碑创建（见 CLAUDE.md），不预建空模块。
"""

__version__ = "0.1.0"
