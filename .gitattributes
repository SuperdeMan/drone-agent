# Keep LF in the working tree on every platform: configs and scripts are consumed inside Linux
# containers, and CRLF churn hides real diffs.
# 所有平台工作树统一 LF：配置与脚本会在 Linux 容器内消费，CRLF 抖动会淹没真实差异。
* text=auto eol=lf
*.png binary
*.jpg binary
*.mcap binary
*.ulg binary
