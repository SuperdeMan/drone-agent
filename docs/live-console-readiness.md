# 实时仿真入口验收（2026-09-21）

本次交付 D027：浏览器发起固定 M1 云端巡检、读取实时机载轨迹和相机、暂停/恢复/取消、拉取正式离线证据页。使用方法见 [实时指南](live-simulation.md)。这次验收不关闭 M2，也不把历史 M1 的 66/66 转借到新提交。

## 版本与部署

| 部件 | 本次版本 |
|---|---|
| 云端运行时与验证镜像 | `cdd57ad0d5cca308a3a1d6dc085c8e1abf04b1b1` |
| 云端部署 ID | `20260921T143223Z-ccf8786a` |
| 控制面 SHA-256 | `7a0bff9c6e22b77f7df0ddc77975bc371f90ff134ed7ccfa8095e4fe442ef9e3` |
| 本机界面 | `d71683f`，包含隐藏链接、整轮失败展示与重连种子回填修正；相对云端运行版本只改 HTML、前端 JavaScript 及其状态测试，Python 与飞行逻辑未变 |

部署回执见 [deployment.json](verification/live-console-2026-09-21-deployment.json)：云端 454 项测试，失败/错误/跳过均为 0；未解锁冒烟通过、控制命令发送数 0；部署前后其他 30 个容器身份摘要相同。入口不开放云主机端口，使用已有 SSH；本机页面服务只绑定 `127.0.0.1:8768`。

本地 `ruff`、454 项 pytest、4 项前端状态测试通过。浏览器自动化工具无法启动 `codex app-server`，因此没有浏览器点击/截图层面的视觉验收；实际功能验证通过页面使用的同一 HTTP API 完成。

## 实时入口验证

| 操作 | 种子 | 结果 |
|---|---|---|
| 正常巡检 | 7 / 41 | 2 次 `completed` |
| 人工暂停、等待、显式恢复 | 7 / 19 / 41 | 3 次 `completed` |
| 巡航期间人工取消 | 7 / 19 / 41 | 3 次 `safe_abort` |

8 轮有效验证全部满足：应用 SHA 一致、独立裁判通过、错误成功报告 0、在线/离线重判一致、终态不在空中且上锁；多次新鲜遥测与不同相机帧可读取，实际操作 ID 都出现在机载接受事件中。重复启动返回同一运行，第二个并发运行被拒；重复操作不延长请求有效期、不重复写入。每轮从实时入口拉取并校验后生成了正式离线证据页。

另有一轮原计划为种子 41 暂停恢复的探针，在本机观察记录中出现约 11 分钟空档，未发出操作，云端任务正常完成。它作为未满足测试步骤的负记录保留，**不计入 8 轮通过数**；随后补跑该种子通过。9 次云端运行合计 189 个裁判覆盖文件重新读取哈希，缺失和不一致均为 0。逐轮检查、负记录、原始云端回执与摘要复核见 [http.json](verification/live-console-2026-09-21-http.json)。

本机入口切换为独立后台进程后，重新读取到同一已完成任务，没有触发新运行，见 [reconnect.json](verification/live-console-2026-09-21-reconnect.json)。本机观察空档不阻止已授权云端任务继续；页面连接本身不是飞行心跳。

## 既有 M1 回归

同一云端运行版本执行 `nominal,cancel_takeoff,cancel_cruise,cancel_landing,pause_resume,pause_timeout` × 种子 `7,19,41`，**18/18 通过**：6 组 `completed`、12 组 `safe_abort`、错误成功报告 0、18 组回放一致。运行 ID：`m1-20260921T150133Z-38f3ea04`。其他 30 个容器前后身份摘要一致。见 [regression.json](verification/live-console-2026-09-21-regression.json)。

回归的 373 个裁判覆盖文件摘要复核全部一致，见 [regression-audit.json](verification/live-console-2026-09-21-regression-audit.json)。8 轮有效 HTTP 验证与 18 组回归共 26 次运行的最差单场景监督周期 p99 为 105.8 ms、最大周期为 114.9 ms，见 [supervision.json](verification/live-console-2026-09-21-supervision.json)。原有飞行新鲜度、暂停上限与安全策略均未放宽。

## 交付状态

最后保留的正常巡检为 `m1-20260921T153418Z-6ac2addf`、种子 41，任务完成并保存照片。之后未解锁环境冒烟再次通过，`armed=false`、探针发送控制命令数 0，见 [post-smoke.json](verification/live-console-2026-09-21-post-smoke.json)。本机后台入口返回 HTTP 200，实际提供的 JavaScript 与 `d71683f` 文件一致；源代码摘要和当前记录见 [entry.json](verification/live-console-2026-09-21-entry.json)。

验证期间发生过短时 SSH 连接失败：只读查询或启动前读状态失败，没有据此重新发起飞行；连接恢复后读回原任务。页面遇到该情况显示断开并禁用操作，恢复后重新读取状态；写请求不自动重发。最终 HTTP 连通性已恢复并核对。

证据总索引与归档文件哈希见 [live-console-2026-09-21.json](verification/live-console-2026-09-21.json)。这是一组受影响场景回归，未在新版本重跑完整 66 组 M1 发布矩阵；完整 M2 控制台与浏览器视觉验收也未在本次关闭。

## 与最近提交的关系

- `fdad577` 拆解 M2–M4 计划；本次按 D027 补遗提供固定任务入口，M2 规划、审批、签名上行与 hri.v0 仍依照原计划推进。
- `461ec85` 提供 `runs`、`fetch` 与只读离线证据浏览器。本次复用其摘要核对、PNG 编码、事件标签与正式证据页生成，没有用实时界面替换裁判。
- `cdd57ad` 增加本机 HTTP/SSH 桥、云端后台任务与操作信箱，以及迟到操作的机载拒绝处理。`8ed82e0` 修正尚不可用的证据链接显示，`e6bfa69` 保留整轮环境验证失败，`d71683f` 在重连时回填当前任务种子；这些本机界面修正不改变云端执行代码。
