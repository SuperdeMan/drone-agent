# M2 评审与云端飞行台合并（2026-09-24）

## 结论与范围

M2 已形成可运行的受约束规划、编译准入、人工审批、签名投递、机载执行与证据报告闭环。本轮评审发现并修复了证据过早报成功、取消后自动重飞、裁判漏检等问题，云端运行版本为 `e8edf28`。历史 M2 发布门禁只属于 `f362b9e`；评审起点为 `dc48b5f`，评审开始时线上为 `74984f9`，所有证据按确切版本区分。

M1/M2 可以合并使用入口。采用 D037：`8448/` 为自然语言任务，`8448/fixed/` 为固定巡检，共用导航、私网身份和工作区锁；保留各自后端与证据裁判，不合并机载控制出口。旧 `8447` 是兼容入口。M1 的本地可信固定包与 M2 的人工审批签名包权限不同，直接把任意 M2 请求送进 M1 代理不可接受。

| 使用语义 | 固定巡检 · M1 | 自然语言任务 · M2 |
|---|---|---|
| 任务来源 | 部署版本自带的固定任务，只能选种子 | 自然语言 → 受约束草案 → 编译准入 |
| 执行授权 | 固定本地可信包，经受限任务代理启动 | 人工审批绑定包哈希，Ed25519 签名与机载验签 |
| 操作 | 支持合适航段的暂停 / 恢复 / 取消 | 按技能清单提供操作；当前巡检包不提供暂停 |
| 主要视图 | 实时轨迹、相机与独立裁判 / 完整证据页 | 规划、审批、版本、操作、三列报告与复核影像 |
| 共享约束 | 与 M2、部署及批次共用项目锁，独立 guardian 出口 | 同左；新版本落地后才切换 |

这一合并不需要新数据库、消息总线、前端框架或第二个控制服务；改动集中在网页路由、导航、挂载与健康核对。若以后要让固定任务也进入统一任务列表，应该把它定义为 M2 的受控模板，并单独验证审批、暂停和历史证据兼容，不能仅迁移按钮。

## 发现与处理

当前部署源码为 `e8edf2831c6f152d62e70f74312d9d8b0e1076fb`（部署 `20260924T040445Z-4693ad39`）。当前候选与中间候选证据分列，见下文。

机器可读总索引：[最终验证摘要](verification/flight-desk-2026-09-24-summary.json)。该摘要逐项核对源码、测试、场景、交互、实调和最终状态，附原始回执摘要。

| 优先级 | 问题及触发方式 | 影响 | 修复与回归 |
|---|---|---|---|
| P1 | 成功事件先于影像上传到达；服务 `_verifications` 跳过缺媒体，报告将缺少服务判定视为可完成 | 没有完成影像复核就出现“已完成”，迟到不一致结果也可能留下成功任务状态 | 影像判定缺省 unknown；等待同步使用 `verifying`，补齐后重推导；`test_delayed_media_cannot_complete_a_mission_and_later_converges` |
| P1 | 实调取消已被机载接受，但多相位巡检最终返回 unknown；服务只依据 cancelled 结果停止重规划 | 用户取消被后续自动批准的版本覆盖，仿真再次起飞 | 服务已排队的取消或机载已接受的取消均阻止自动重规划；原始 unknown 不变；分别回归两类取消来源及普通暂停不受影响 |
| P1 | 原独立裁判未核对取消后的新版本飞行 | 后续物理效果真实也会被记为整体通过，取消违约只能由专项探针发现 | 裁判独立检查取消版本与后续飞行版本；在线 / 回放均拒绝取消后的复飞，普通暂停对照仍通过 |
| P1 | 服务接受缺失原始字段、摘要错误的事件；完整性检查仅在展示中，账本缺行仍可推进 | 不完整镜像可以产生完成、操作绑定或自动重规划 | 入账校验行摘要和事件身份；两本账本连续受检才驱动决定；缺行补齐后收敛；`test_journal_gap_blocks_completion_until_the_missing_row_arrives` 等 |
| P2 | 至少一次上传重发相同 Evidence，SQLite `INSERT OR REPLACE` 把媒体关联替换为 null | 媒体已持久化却在重试后丢失，影响复核与查看 | 相同元数据幂等，冲突内容拒绝，媒体引用保留；`test_duplicate_evidence_keeps_uploaded_media_and_rejects_conflicts` |
| P1 | 原证据 ID 仅由像素哈希构成；不同任务 / 版本采集相同像素 | 后一次上传可能覆盖前一个任务的证据；单纯拒绝冲突又会使后任务永远缺证据 | uplink 按机器人、任务、版本及完整证据契约生成采集 ID，像素哈希仅用于媒体去重；跨任务相同像素回归确保两份证据独立且均完成 |
| P2 | 服务已确认上传、尚未生成报告就重启；内存中的待复核集合丢失 | 没有新事件时报告可能永久停留在旧状态 | 启动时重新推导持久化任务；`test_restart_reconciles_uploaded_inputs_without_waiting_for_new_events` |
| P2 | 自动审批只比较技能、版本和参数，未比较证据要求、资源和超时 | 不符合“原样重试”的包被分类为自动批准；当前编译器通常会重建这些字段，属于独立防线缺口 | 比较完整执行绑定与证据要求；三类修改回归分别要求人工审批 |
| P2 | 影像复核按坐标数值计算距离，未比对坐标系 / 地图版本 | 来自另一坐标系或地图的位姿可能通过服务复核 | 与审批包的 frame / map_version 比对；对应回归拒绝两类不匹配 |
| P2 | M2 规划中版本列表可能为空，页面直接访问版本字段；断线保留写按钮 | 页面报错或操作者误以为点击已提交 | 空版本显示规划中；断线禁用写按钮并标明历史状态 |
| P1 | guardian 退出时先等待 RPC 停止，期间监督 / 观测任务仍运行；SITL 实测已落地的末帧随后变为 unknown | 独立裁判无法确认终态，落地后还出现额外恢复事件 | 先停止监督 / 观测发布，再关闭 RPC 与适配器；增加停机竞态回归，裁判和新鲜度阈值不变 |
| P2 | M2 编排未在裁判失败后停止剩余用例，与操作指南不一致 | 已失败的批次仍派发后续飞行，扩大排查面 | 首次失败即停止后续种子和场景，保留批次回执并恢复空闲仿真；增加失败停止回归 |

第一批新增回归在修复前准确失败 8 项（迟到媒体、账本缺行、重复证据、两种坏事件、三种自动审批漏比），修复后通过。坐标系与界面问题另有检查。未改变契约测试断言、飞控失效保护或 guardian 阈值；没有数据库 schema 迁移。

取消守卫读取服务中的操作者请求和 executive 的取消接受事件。guardian 内部也使用 cancel 进行失败恢复，不能仅凭它的 `user_cancel` 原因字符串推断“操作者取消”，否则会错误阻断正常重规划。

## 架构评估

- 规划层：模型只返回草案；检索只读，结构化输出校验和有限重试不授予执行权限。脚本回答与 MiniMax 实调需要分别报告。
- 准入层：登记的空间、能力、时间、能耗与参数独立检查；空域桩拒绝真实模式，仍只适用于 SITL。
- 执行层：uplink、executive、guardian 分别验签，executive 无网络，guardian 是唯一飞控出口；重规划只在落地后以新代次接管。
- 服务层：账本镜像最终一致，服务停机不影响已授权飞行；本轮补齐了镜像完整性与影像迟到的状态收敛约束。
- 合并范围：网页增加既有 M1 代理 socket、只读运行与版本目录、独立证据页面缓存。没有 Docker socket、SSH 私钥、签名私钥、模型 key 或仿真网络。固定模式 POST 与 M2 一样要求已识别操作者，并继续校验 Origin / nonce。

## 当前限制与后续工作

M2 仍是单机、登记航线巡检；局部自主、真机、多机器人均未交付。M2 巡检步骤没有可暂停能力，不能因为 M1 有暂停按钮就宣称两者等价。排队但未起飞的 M2 任务撤回仍未实现，需要单独定义撤销投递与 inbox 的一致性。规划过程中服务重启的恢复、长期证据保留策略和多用户权限应在扩大常驻使用前单独设计。

本轮不重跑 M1 66/66 或 32 条模型准入率基线，不将历史门禁转借给新版本。

## 最终验证（e8edf28）

| 检查 | 结果 / 边界 | 证据 |
|---|---|---|
| 本机 | ruff 通过；833 项中 832 通过、1 项 Windows Unix socket 跳过 | [local](verification/flight-desk-2026-09-24-e8edf28-local.json) |
| 云端 | Linux 833/833，无失败 / 错误 / 跳过；未解锁冒烟通过；其他 30 个容器身份前后一致 | [deployment](verification/flight-desk-2026-09-24-e8edf28-deployment.json) |
| 激活 | M1 代理、统一网页与 M2 服务同版本；实际挂载、网络、权限与独立 Serve 映射核对通过 | [console](verification/flight-desk-2026-09-24-e8edf28-console-activation.json)、[desk](verification/flight-desk-2026-09-24-e8edf28-desk-activation.json) |
| 入口协议 | M1/M2 页面、资源、状态均可访问；A2A 未授权 401、跨源 WS 403、伪造身份未生效 | [HTTP](verification/flight-desk-2026-09-24-e8edf28-http.json)、[身份](verification/flight-desk-2026-09-24-e8edf28-spoof.json) |
| MiniMax 实调取消 | `m-61d08a5541b1`：机载明确接受取消，v1 巡检步骤保持 unknown；仅一个版本、重规划 0、报告“不确定”；裁判未完成、通过且回放一致 | [cancel](verification/flight-desk-2026-09-24-e8edf28-live-cancel.json) |
| 旧错误行为复判 | 新裁判对旧 `58b3cdc` 任务 `m-93ff3ec41aa5` 判不通过，问题为 `v1:flight_after_operator_cancel`；原始旧结果保留 | [rejudge](verification/flight-desk-2026-09-24-e8edf28-cancel-rejudge.json) |
| 完整 M2 E2E 子门禁 | 6 场景 × 3 种子 = 18/18：12 完成、6 合理不飞，错误成功 0、在线 / 回放全部一致；12 份计入回执均通过隔离检查；脚本规划回答 | [E2E 子门禁与输入摘要](verification/flight-desk-2026-09-24-e8edf28-e2e-gate.json) |
| 实调隐私拒答 | `m-e6c5ddff2fd1`：MiniMax-M3 拒答，未审批、未飞行 | [privacy](verification/flight-desk-2026-09-24-e8edf28-live-privacy.json) |
| 原始产物复核 | 最终版本重规划种子 19 的 52 份文件摘要一致；v1 未证实 / v2 已证实；epoch 1 → 2；两版均落地上锁，策略审批不超出人工审批有效期 | [拉取](verification/flight-desk-2026-09-24-e8edf28-fetch.json)、[逐字段核对](verification/flight-desk-2026-09-24-e8edf28-artifact-audit.json) |
| M1 统一入口交互 | `/fixed/` 经真实 HTTPS：正常种子 7 完成、暂停 / 恢复种子 19 完成、取消种子 41 安全中止；操作均被机载接受，错误成功 0，回放一致，三份完整证据页均 200 | [正常](verification/flight-desk-2026-09-24-e8edf28-fixed-nominal.json)、[暂停](verification/flight-desk-2026-09-24-e8edf28-fixed-pause.json)、[取消](verification/flight-desk-2026-09-24-e8edf28-fixed-cancel.json) |
| MiniMax 正向实调 | 红色 `m-0ed01b54405f`、蓝色 `m-af80d8fda753` 均完成，无重规划，裁判通过、错误成功 0、回放一致 | [红色](verification/flight-desk-2026-09-24-e8edf28-live-red.json)、[蓝色](verification/flight-desk-2026-09-24-e8edf28-live-blue.json) |
| 实调来源 | 4 份原始录制均为 recorded / minimax、软件 e8edf28；共 4 次交互，输入 7,484 / 输出 823 token；未核实价格，费用记为 unpriced | [录制元数据与摘要](verification/flight-desk-2026-09-24-e8edf28-live-provenance.json) |
| 最终状态 | 未解锁冒烟通过；M1 入口与统一入口均 ready、同版本；监管者 idle、队列为空 | [冒烟](verification/flight-desk-2026-09-24-e8edf28-post-smoke.json)、[最终状态](verification/flight-desk-2026-09-24-e8edf28-final-status.json) |

E2E 子门禁调用现有 `verify_m2_release.py` 的 E2E 判据，核对确切版本、完整场景集合、签名 / 证书记录、逐例裁判与回放、错误成功和隔离摘要。它不是完整 M2 发布门禁；本轮没有重新运行 M1 66/66 和 32 条模型准入率基线。

浏览器视觉验收未完成：初次没有可用 Chrome / 内置浏览器，后来可枚举 Edge，但打开和读取页面均报控制连接失败。已执行页面/脚本 HTTPS 核对、hri.v0 协议及前端状态检查，但不宣称截图或浏览器点击验收通过。

## 开发负结果

候选 `7187c7e` 的批次 `m2-20260924T025613Z-a578a9ed` 在红色巡检种子 41 遇到 `v1:missing_grounded_disarmed_terminal`，在线 / 回放一致拒收、错误成功报告 0。MCAP 中最后一个明确落地上锁样本为 `03:00:40.486327Z`（定位健康、电量 0.98）；最终样本 `03:00:41.183758Z` 的 armed / in_air / 电量均为 null。PX4 日志有 `Landing detected` 与 `Disarmed by landing`，guardian 也有 `landed_disarmed`，问题在停机期间的末帧覆写，而非通过改判掩盖未落地。

发现编排没有失败停止后，暂停调度进程，等待当时已启动的蓝色种子 41 完成并观测落地上锁，再终止调度、停止本项目临时飞行容器、恢复空闲仿真。失败前后的已运行结果保留，停止时未裁判的架次不计为通过。见 [负结果](verification/flight-desk-2026-09-24-m2-initial-negative.json)、[停机诊断](verification/flight-desk-2026-09-24-shutdown-diagnosis.json)。`58b3cdc` 修正退出顺序与批次失败停止，后续验收另算。

## 中间候选验证证据

开发过程中一条中文 stdin 探针因 PowerShell 默认编码变成问号（`release-live-refused.json`），仅记录了“输入不可理解”的拒答，不计为隐私拒答样本。后续探针显式使用 UTF-8，并核对服务保存的请求原文；最终隐私样本以 `e8edf28-live-privacy.json` 为准。

`58b3cdc` 的 18/18 M2 脚本规划矩阵与 M1 三种交互通过后，MiniMax 取消专项发现新的问题：任务 `m-93ff3ec41aa5` 在 `03:49:17.334825Z` 接受取消，步骤于 `03:49:23.262809Z` 以 unknown 结束，服务却自动批准 v2 并再次飞行。原取消探针因此失败，独立裁判的“完成”只证明后续飞行与报告一致，不能证明取消语义正确。见 [失败探针](verification/flight-desk-2026-09-24-release-live-cancel.json)、[取消接受与未知结果的账本摘录](verification/flight-desk-2026-09-24-cancel-negative-events.json)。摘录已脱敏，所带行摘要指向原始远端行，不代表脱敏文本的摘要。

`8262984` 对现有重规划拦截增加取消意图检查；新增回归在修复前准确失败 2 项，普通暂停对照通过。后续证据单独绑定该提交，不把 `58b3cdc` 的 18/18 转借给它。

`e8edf28` 进一步把“取消后不得再飞更高版本”加入独立裁判，避免仅靠调用探针的额外断言发现此类违约。

该版本首轮重规划批次 `m2-20260924T041207Z-79f407d6` 的种子 19 在 v2 遇到定位 / 能源遥测未知、v3 遇到观测过期，按原策略安全中止，错误成功 0，但版本数和最终状态不符合场景期望，批次自动停止。同一时间窗内其他 27 个容器于 04:19 UTC 启动，其他容器身份摘要前后不同；资源争用是有依据的推测，不能当作已独立证明的根因。保留 [环境负记录](verification/flight-desk-2026-09-24-e8edf28-environment-negative.json) 与 [失败批次](verification/flight-desk-2026-09-24-e8edf28-m2-replan_degraded_image.json)，重规划改按单种子批次重跑，源码、阈值和裁判不变。

运行代码：`7187c7ed2c88461dd31f4409bbd979a736c5ce4e`；部署 ID：`20260924T025228Z-52933ad1`；控制面 SHA-256：`6d9f3c9911b4068c424cb6dbd19c133286a45caed9adddb02160a05c953da139`。后续文档归档提交与运行版本分开。

| 检查 | 结果 / 边界 | 证据 |
|---|---|---|
| 本机检查 | ruff 通过；826 项中 825 通过、1 项 Windows 不支持的 Unix socket 测试跳过；M1 前端状态测试 4/4 | [local.json](verification/flight-desk-2026-09-24-local.json) |
| 云端 Linux 检查 | 826/826，无失败 / 错误 / 跳过；未解锁冒烟通过；其他 30 个容器身份前后一致 | [deployment.json](verification/flight-desk-2026-09-24-final-deployment.json) |
| 统一入口激活 | M1 代理、统一页面、M2 服务版本一致；容器挂载 / 网络 / 权限与 Serve 隔离核对通过 | [M1 代理](verification/flight-desk-2026-09-24-final-console-activation.json)、[任务台](verification/flight-desk-2026-09-24-final-desk-activation.json) |
| HTTPS / 身份 | M1/M2 页面、资源及健康均 200；A2A 未授权 401，跨源 WS 403，伪造身份头未生效 | [HTTP](verification/flight-desk-2026-09-24-http.json)、[身份](verification/flight-desk-2026-09-24-spoof.json) |
| MiniMax 拒答 | 场地外且涉及隐私的请求一次拒答；没有审批、任务包、飞行与证据 | [实调拒答](verification/flight-desk-2026-09-24-live-refused.json) |

上述表格仅属于中间候选，不能作为当前运行源码的验证结论。
